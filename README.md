# CodeAlpha_URL_Shorter
A simple URL shortener built using Flask/Express.js. It generates short unique codes for long URLs, stores them in a database, and redirects users to the original links. Includes an optional frontend to create and view short URLs.
